# @package _global_

defaults:
  - _self_
  - data: flight.yaml # choose datamodule with `test_dataloader()` for evaluation
  - model: flight.yaml
  - logger: null # csv.yaml
  - paths: default.yaml
  - extras: default.yaml
  - hydra: default.yaml
  - trainer: gpu.yaml # default.yaml

task_name: "eval"

tags: ["dev"]

seed: 42

# passing checkpoint path is necessary for evaluation
ckpt_path: ???
